# Shell scripts must stay LF so the Docker image's shebang works even though the
# repo is developed on Windows (core.autocrlf=true would otherwise inject CRLF).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
